import { StyleSheet } from "react-native";

export const LevelQuizStyle = StyleSheet.create({
  CloseButton: {
    display: "flex",
    alignItems: "flex-end",
    padding: 5,
  },
  NextButton: {
    display: "flex",
    alignItems: "flex-end",
  },
  ContainerOptions: {
    width: "100%",
    paddingHorizontal: 30,
    gap: 12,
    marginTop: 10,
  },
});
