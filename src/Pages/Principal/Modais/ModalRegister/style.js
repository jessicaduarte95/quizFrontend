import { StyleSheet } from 'react-native';

export const ModalRegister = StyleSheet.create({
	ContainerButton: {
		flexDirection: 'row',
		justifyContent: 'flex-end',
        marginTop: 16,
        gap: 10
	}
});
